import React from 'react';
import Feed from '../components/feed';

export default class FeedScreen extends React.Component {
    render() {
        return (
            <Feed filterType="none" filter="none"/>
        )
    }
}